package org.apache.turbine.test.integrationtest.om;

import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.List;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.apache.torque.Column;
import org.apache.torque.TorqueException;
import org.apache.torque.criteria.CriteriaInterface;
import org.apache.torque.om.mapper.RecordMapper;


/**
 * Maps ResultSet rows into TurbineUser objects.
 *
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [Thu Oct 04 13:45:56 EDT 2018]
 *
 */
public class BaseTurbineUserRecordMapper implements RecordMapper<TurbineUser>
{

    /** The class log. */
    private static Log log
            = LogFactory.getLog(BaseTurbineUserRecordMapper.class);

    /**
     * Constructs the object from the current row in the resultSet.
     *
     * @param resultSet the resultSet to operate on, already pointing
     *        to the correct row. Not null.
     * @param a possible offset in the columns to be considered
     *        (if previous columns contain other objects),
     *        or 0 for no offset.
     * @param criteria The criteria which created the result set.
     *        If set, the attributes to set in the data object
     *        are determined from the select columns in the criteria;
     *        if no matching column can be found, null is returned.
     *        If not set, all of the table's columns are read from the
     *        result set in the order defined in the table definition.
     *
     * @return the mapped object, not null.
     *
     * @throws TorqueException when reading fields from the RecordSet fails
     *         or if a Criteria is passed which contains select columns other
     *         than the columns in the TURBINE_USER table.
     */
    public TurbineUser processRow(
                ResultSet resultSet,
                int offset,
                CriteriaInterface<?> criteria)
            throws TorqueException
    {
        TurbineUser turbineUser = new TurbineUser();

        try 
        {
            turbineUser.setLoading(true);
            if (criteria == null)
            {
                turbineUser.setEntityId(
                        getEntityId(resultSet, offset + 1));
                turbineUser.setEntityName(
                        getEntityName(resultSet, offset + 2));
                turbineUser.setPassword(
                        getPassword(resultSet, offset + 3));
                turbineUser.setFirstName(
                        getFirstName(resultSet, offset + 4));
                turbineUser.setLastName(
                        getLastName(resultSet, offset + 5));
                turbineUser.setEmail(
                        getEmail(resultSet, offset + 6));
                turbineUser.setConfirmed(
                        getConfirmed(resultSet, offset + 7));
                turbineUser.setModifiedDate(
                        getModifiedDate(resultSet, offset + 8));
                turbineUser.setCreateDate(
                        getCreateDate(resultSet, offset + 9));
                turbineUser.setLastLogin(
                        getLastLogin(resultSet, offset + 10));
                turbineUser.setObjectdata(
                        getObjectdata(resultSet, offset + 11));
            }
            else
            {
                // try to get columns to be mapped
                // from criteria's select columns
                boolean columnMapped = false;
                int totalOffset = offset + 1;
                List<Column> selectColumns = criteria.getSelectColumns();
                List<Column> columnsWithoutOffset = selectColumns.subList(
                        offset, 
                        selectColumns.size());
                for (Column column : columnsWithoutOffset)
                {
                    if (BaseTurbineUserPeer.USER_ID.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        turbineUser.setEntityId(
                            getEntityId(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseTurbineUserPeer.LOGIN_NAME.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        turbineUser.setEntityName(
                            getEntityName(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseTurbineUserPeer.PASSWORD_VALUE.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        turbineUser.setPassword(
                            getPassword(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseTurbineUserPeer.FIRST_NAME.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        turbineUser.setFirstName(
                            getFirstName(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseTurbineUserPeer.LAST_NAME.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        turbineUser.setLastName(
                            getLastName(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseTurbineUserPeer.EMAIL.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        turbineUser.setEmail(
                            getEmail(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseTurbineUserPeer.CONFIRM_VALUE.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        turbineUser.setConfirmed(
                            getConfirmed(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseTurbineUserPeer.MODIFIED_DATE.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        turbineUser.setModifiedDate(
                            getModifiedDate(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseTurbineUserPeer.CREATED.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        turbineUser.setCreateDate(
                            getCreateDate(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseTurbineUserPeer.LAST_LOGIN.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        turbineUser.setLastLogin(
                            getLastLogin(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    else if (BaseTurbineUserPeer.OBJECTDATA.getSqlExpression().equals(
                            column.getSqlExpression()))
                    {
                        turbineUser.setObjectdata(
                            getObjectdata(resultSet, totalOffset));
                        columnMapped = true;
                    }
                    totalOffset++;
                }
                if (!columnMapped)
                {
                    log.debug("no columns to map found in criteria, "
                        + "returning null");
                    return null;
                }
            }
            turbineUser.setNew(false);
            turbineUser.setModified(false);
        }
        finally
        {
            turbineUser.setLoading(false);
        }
        return turbineUser;
    }

        /**
         * Reads the value of the <code>columnIndex</code>'th column
         * in the <code>resultSet</code> so that it can be used to set
         * the field entityId in TurbineUser.
         *
         * @param dbObject the database object to fill, not null.
         * @param resultSet the ResultSet to read from, not null.
         * @param columnIndex the index in the resultSet which should be read.
         *
         * @return the content of the column.
         *
         * @throws SQLException if a problem occurs when reading
         *         from the resultSet.
         */
        protected Integer getEntityId(
                    ResultSet resultSet,
                    int columnIndex)
                throws TorqueException
        {
            try
            {
                Integer value = resultSet.getInt(columnIndex);
                if (resultSet.wasNull())
                {
                    value = null;
                }
                return value;
            }
            catch (SQLException e)
            {
                throw new TorqueException(e);
            }
        }

        /**
         * Reads the value of the <code>columnIndex</code>'th column
         * in the <code>resultSet</code> so that it can be used to set
         * the field entityName in TurbineUser.
         *
         * @param dbObject the database object to fill, not null.
         * @param resultSet the ResultSet to read from, not null.
         * @param columnIndex the index in the resultSet which should be read.
         *
         * @return the content of the column.
         *
         * @throws SQLException if a problem occurs when reading
         *         from the resultSet.
         */
        protected String getEntityName(
                    ResultSet resultSet,
                    int columnIndex)
                throws TorqueException
        {
            try
            {
                return resultSet.getString(columnIndex);
            }
            catch (SQLException e)
            {
                throw new TorqueException(e);
            }
        }

        /**
         * Reads the value of the <code>columnIndex</code>'th column
         * in the <code>resultSet</code> so that it can be used to set
         * the field password in TurbineUser.
         *
         * @param dbObject the database object to fill, not null.
         * @param resultSet the ResultSet to read from, not null.
         * @param columnIndex the index in the resultSet which should be read.
         *
         * @return the content of the column.
         *
         * @throws SQLException if a problem occurs when reading
         *         from the resultSet.
         */
        protected String getPassword(
                    ResultSet resultSet,
                    int columnIndex)
                throws TorqueException
        {
            try
            {
                return resultSet.getString(columnIndex);
            }
            catch (SQLException e)
            {
                throw new TorqueException(e);
            }
        }

        /**
         * Reads the value of the <code>columnIndex</code>'th column
         * in the <code>resultSet</code> so that it can be used to set
         * the field firstName in TurbineUser.
         *
         * @param dbObject the database object to fill, not null.
         * @param resultSet the ResultSet to read from, not null.
         * @param columnIndex the index in the resultSet which should be read.
         *
         * @return the content of the column.
         *
         * @throws SQLException if a problem occurs when reading
         *         from the resultSet.
         */
        protected String getFirstName(
                    ResultSet resultSet,
                    int columnIndex)
                throws TorqueException
        {
            try
            {
                return resultSet.getString(columnIndex);
            }
            catch (SQLException e)
            {
                throw new TorqueException(e);
            }
        }

        /**
         * Reads the value of the <code>columnIndex</code>'th column
         * in the <code>resultSet</code> so that it can be used to set
         * the field lastName in TurbineUser.
         *
         * @param dbObject the database object to fill, not null.
         * @param resultSet the ResultSet to read from, not null.
         * @param columnIndex the index in the resultSet which should be read.
         *
         * @return the content of the column.
         *
         * @throws SQLException if a problem occurs when reading
         *         from the resultSet.
         */
        protected String getLastName(
                    ResultSet resultSet,
                    int columnIndex)
                throws TorqueException
        {
            try
            {
                return resultSet.getString(columnIndex);
            }
            catch (SQLException e)
            {
                throw new TorqueException(e);
            }
        }

        /**
         * Reads the value of the <code>columnIndex</code>'th column
         * in the <code>resultSet</code> so that it can be used to set
         * the field email in TurbineUser.
         *
         * @param dbObject the database object to fill, not null.
         * @param resultSet the ResultSet to read from, not null.
         * @param columnIndex the index in the resultSet which should be read.
         *
         * @return the content of the column.
         *
         * @throws SQLException if a problem occurs when reading
         *         from the resultSet.
         */
        protected String getEmail(
                    ResultSet resultSet,
                    int columnIndex)
                throws TorqueException
        {
            try
            {
                return resultSet.getString(columnIndex);
            }
            catch (SQLException e)
            {
                throw new TorqueException(e);
            }
        }

        /**
         * Reads the value of the <code>columnIndex</code>'th column
         * in the <code>resultSet</code> so that it can be used to set
         * the field confirmed in TurbineUser.
         *
         * @param dbObject the database object to fill, not null.
         * @param resultSet the ResultSet to read from, not null.
         * @param columnIndex the index in the resultSet which should be read.
         *
         * @return the content of the column.
         *
         * @throws SQLException if a problem occurs when reading
         *         from the resultSet.
         */
        protected String getConfirmed(
                    ResultSet resultSet,
                    int columnIndex)
                throws TorqueException
        {
            try
            {
                return resultSet.getString(columnIndex);
            }
            catch (SQLException e)
            {
                throw new TorqueException(e);
            }
        }

        /**
         * Reads the value of the <code>columnIndex</code>'th column
         * in the <code>resultSet</code> so that it can be used to set
         * the field modifiedDate in TurbineUser.
         *
         * @param dbObject the database object to fill, not null.
         * @param resultSet the ResultSet to read from, not null.
         * @param columnIndex the index in the resultSet which should be read.
         *
         * @return the content of the column.
         *
         * @throws SQLException if a problem occurs when reading
         *         from the resultSet.
         */
        protected java.util.Date getModifiedDate(
                    ResultSet resultSet,
                    int columnIndex)
                throws TorqueException
        {
            try
            {
                java.util.Date value = resultSet.getTimestamp(columnIndex);
                if (resultSet.wasNull())
                {
                    value = null;
                }
                return value;
            }
            catch (SQLException e)
            {
                throw new TorqueException(e);
            }
        }

        /**
         * Reads the value of the <code>columnIndex</code>'th column
         * in the <code>resultSet</code> so that it can be used to set
         * the field createDate in TurbineUser.
         *
         * @param dbObject the database object to fill, not null.
         * @param resultSet the ResultSet to read from, not null.
         * @param columnIndex the index in the resultSet which should be read.
         *
         * @return the content of the column.
         *
         * @throws SQLException if a problem occurs when reading
         *         from the resultSet.
         */
        protected java.util.Date getCreateDate(
                    ResultSet resultSet,
                    int columnIndex)
                throws TorqueException
        {
            try
            {
                java.util.Date value = resultSet.getTimestamp(columnIndex);
                if (resultSet.wasNull())
                {
                    value = null;
                }
                return value;
            }
            catch (SQLException e)
            {
                throw new TorqueException(e);
            }
        }

        /**
         * Reads the value of the <code>columnIndex</code>'th column
         * in the <code>resultSet</code> so that it can be used to set
         * the field lastLogin in TurbineUser.
         *
         * @param dbObject the database object to fill, not null.
         * @param resultSet the ResultSet to read from, not null.
         * @param columnIndex the index in the resultSet which should be read.
         *
         * @return the content of the column.
         *
         * @throws SQLException if a problem occurs when reading
         *         from the resultSet.
         */
        protected java.util.Date getLastLogin(
                    ResultSet resultSet,
                    int columnIndex)
                throws TorqueException
        {
            try
            {
                java.util.Date value = resultSet.getTimestamp(columnIndex);
                if (resultSet.wasNull())
                {
                    value = null;
                }
                return value;
            }
            catch (SQLException e)
            {
                throw new TorqueException(e);
            }
        }

        /**
         * Reads the value of the <code>columnIndex</code>'th column
         * in the <code>resultSet</code> so that it can be used to set
         * the field objectdata in TurbineUser.
         *
         * @param dbObject the database object to fill, not null.
         * @param resultSet the ResultSet to read from, not null.
         * @param columnIndex the index in the resultSet which should be read.
         *
         * @return the content of the column.
         *
         * @throws SQLException if a problem occurs when reading
         *         from the resultSet.
         */
        protected byte[] getObjectdata(
                    ResultSet resultSet,
                    int columnIndex)
                throws TorqueException
        {
            try
            {
                byte[] value = resultSet.getBytes(columnIndex);
                if (resultSet.wasNull())
                {
                    value = null;
                }
                return value;
            }
            catch (SQLException e)
            {
                throw new TorqueException(e);
            }
        }


}
